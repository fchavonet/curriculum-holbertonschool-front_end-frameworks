# 3. Final assessment

![Quiz mème](https://raw.githubusercontent.com/fchavonet/curriculum-holbertonschool-front_end-frameworks/refs/heads/main/week-02-vue/assets/images/memes/meme-quiz.webp)

---

The purpose of this assessment is to evaluate your understanding of **Svelte** and your ability to **compare it with React**.

The questions are **based on the concepts explored throughout the project**, the migration process, and the framework analysis performed in the previous task.

---

## Requirements

- The assessment must be completed **individually**.
- Students must be able to **justify and explain their answers**.
- The assessment **focuses on understanding** rather than memorization.
- The assessment **contributes to the final evaluation** of the project.

---

## Quiz

1. What is the main purpose of this Svelte project?

- Learning Svelte by recreating the same application previously built with React and Vue.js. ✅
- Creating a completely different application with a new design.
- Replacing the React and Vue.js projects.
- Learning backend development with Svelte.

2. Why can this Svelte project feel repetitive after the Vue.js project?

- Because repetition helps compare frameworks on the same interface. ✅
- Because the goal is to reinforce framework migration skills. ✅
- Because all frontend frameworks are identical.
- Because the project intentionally reuses the same application structure. ✅

3. Which statements correctly describe Svelte?

- Svelte is a frontend JavaScript framework. ✅
- Svelte uses `.svelte` component files. ✅
- Svelte requires JSX by default.
- Svelte can be used with Vite. ✅

4. What is one important difference between React and Svelte?

- React commonly uses JSX, while Svelte uses its own template syntax. ✅
- React is only used for backend applications.
- Svelte does not support components.
- Svelte requires Vue templates.

5. Which files are typically used as entry points in the Svelte project created with Vite?

- `src/main.js`. ✅
- `src/App.svelte`. ✅
- `src/App.jsx`.
- `src/main.vue`.

6. During the migration from React to Svelte, what usually happens to React component files?

- `.jsx` component files are converted into .svelte component files. ✅
- React components are automatically deleted without replacement.
- Component logic and markup are adapted to Svelte syntax. ✅
- The component-based structure can often be preserved. ✅

7. Which syntax is used by Svelte for conditional rendering?

- `{#if condition}`. ✅
- `v-if="condition"`.
- `condition && (...)`.
- `if condition`.

8. Which syntax is used by Svelte for dynamic list rendering?

- `{#each items as item}`. ✅
- `v-for="item in items"`.
- `items.map()`.
- `forEach={items}`.

9. Which comparison is correct?

- React often uses `.map()` for list rendering. ✅
- Vue uses `v-for` for list rendering. ✅
- Svelte uses `{#each}` for list rendering. ✅
- Svelte requires SQL queries for list rendering.

10. What is the role of props in React, Vue.js and Svelte?

- They allow data to be passed from a parent component to a child component. ✅
- They are used to define CSS media queries.
- They help make components reusable. ✅
- They replace all local state.

11. In Svelte 5, which feature can be used to receive component props?

`$props()`. ✅
`defineProps()`.
`useProps()`.
`setProps()`.

12. Which statement best describes Svelte reactivity?

- Svelte can update the UI when reactive data changes. ✅
- Svelte never updates the UI automatically.
- Svelte reactivity is only available inside CSS files.
- Svelte requires React hooks to update the UI.

13. In Svelte 5, which feature can be used to declare reactive local state?

`$state()`. ✅
`useState()`.
`ref()`.
`createStoreOnly()`.

14. Which comparison is correct?

- React can use useState for local state. ✅
- Vue can use ref for reactive state. ✅
- Svelte can use $state for reactive local state. ✅
- All three frameworks use the exact same state syntax.

15. What is the closest Svelte equivalent to React's useEffect used for logic after the component is mounted?

- `onMount`. ✅
- `onMounted`.
- `mounted()`.
- `useMounted`.

16. Which comparison is correct for lifecycle logic?

- React can use `useEffect`. ✅
- Vue can use `onMounted`. ✅
- Svelte can use `onMount`. ✅
- Lifecycle logic is unnecessary in every frontend application.

17. What is `bind:value` commonly used for in Svelte?

- Binding form input values to state. ✅
- Importing components.
- Creating production builds.
- Replacing CSS classes.

18. Which comparison is correct for form management?

- React often uses controlled inputs with state and event handlers. ✅
- Vue can use `v-model`. ✅
- Svelte can use `bind:value`. ✅
- Forms cannot be managed in Svelte.

- Which library replaces `lucide-react` in the Svelte version?

- `@lucide/svelte`. ✅
- `lucide-vue-next`.
- `react-icons`.
- `svelte-icons-react`.

19. Why was keeping a similar project structure useful during the migration?

- It made the code easier to compare across frameworks. ✅
- It helped AI tools understand the source project more clearly. ✅
- It reduced the need to rethink the whole application architecture. ✅
- It made testing and manual review easier. ✅

21. During an AI-assisted framework migration, which actions remain the developer's responsibility?

- Reviewing the generated code. ✅
- Testing the application. ✅
- Understanding the framework-specific syntax. ✅
- Blindly accepting every generated file.

22. Why is a clean React project easier to migrate to Vue.js or Svelte?

- Because reusable components are easier to convert. ✅
- Because clear data files and services can often be reused or adapted. ✅
- Because well-organized code gives better context to AI tools. ✅
- Because messy code always produces perfect migrations.

23. Which statements correctly describe the relationship between React, Vue.js and Svelte?

- They can all be used to build component-based user interfaces. ✅
- They share concepts such as components, props and reactive UI updates. ✅
- They use different syntaxes and development patterns. ✅
- They are all backend database engines.

24. What should students focus on when reading AI-generated Svelte code?

- Understanding how the generated code works. ✅
- Comparing it with the React and Vue.js versions. ✅
- Checking that the UI and behavior remain equivalent. ✅
- Ignoring the code because AI generated it.

25. What is one major professional lesson from this project?

- Knowing one frontend framework well can help you understand others faster. ✅
- AI can help reduce the barrier between frontend frameworks. ✅
- Developers still need to read, debug and validate generated code. ✅
- Framework syntax is the only skill that matters in modern frontend development.