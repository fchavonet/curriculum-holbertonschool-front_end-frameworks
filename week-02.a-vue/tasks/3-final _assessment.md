# 3. Final assessment

![Quiz mème](../assets/images/memes/meme-quiz.webp)

---

The purpose of this assessment is to evaluate your understanding of **Vue.js** and your ability to **compare it with React**.

The questions are **based on the concepts explored throughout the project**, the migration process, and the framework analysis performed in the previous task.

---

## Requirements

- The assessment must be completed **individually**.
- Students must be able to **justify and explain their answers**.
- The assessment **focuses on understanding** rather than memorization.
- The assessment **contributes to the final evaluation** of the project.

---

## Quiz

1. What is Vue.js?

- A CSS framework.
- A frontend JavaScript framework. ✅
- A database engine.
- A backend runtime.

2. Why do multiple frontend frameworks exist?

- Because each framework targets different programming languages.
- Because there is only one valid way to build interfaces.
- Because different communities and teams make different design choices. ✅
- Because browsers require different frameworks.

3. Which concepts are shared by both React and Vue.js?

- Components. ✅
- Reactive UI updates. ✅
- State management. ✅
- SQL queries.

4. What is a Vue Single File Component (SFC)?

- A component stored in a .vue file. ✅
- A component stored in a .jsx file.
- A Vue database model.
- A Vite configuration file.

5. Which statements are true about frontend frameworks?

- They help structure user interfaces. ✅
- They replace HTML completely.
- They encourage component reuse. ✅
- They solve every frontend problem automatically.

6. Which Vue feature is primarily used to create reactive state?

`useState()`.
`ref()`. ✅
`setState()`.
`createState()`.

7. What can be stored inside a `ref`?

- Numbers. ✅
- Strings. ✅
- Objects. ✅
- Arrays. ✅

8. What is the closest Vue equivalent to React's `useEffect` hook?

- `v-if`.
- `v-for`.
- `onMounted`.  ✅
- `defineProps`.

9. What is `v-model` primarily used for?

- Conditional rendering.
- Two-way form binding. ✅
- API requests.
- Component imports.

10. What is the closest React equivalent to `v-model`?

- `useState` combined with event handlers. ✅
- `useEffect`.
- `map()`.
- Props.

11. Which Vue directive is used for list rendering?

- `v-if`.
- `v-bind`.
- `v-for`. ✅
- `v-model`.

12. Which React method is most commonly replaced by v-for during migration?

- `filter()`.
- `reduce()`.
- `map()`. ✅
- `find()`.

13. Which statements are true about JSX?

- JSX is JavaScript syntax. ✅
- JSX mixes markup and logic. ✅
- JSX is mandatory in Vue.
- JSX is commonly used in React. ✅

14. Which statements are true about Vue templates?

- They use directives such as `v-if` and `v-for`. ✅
- They are closer to HTML syntax. ✅
- They require JSX.
- They support interpolation using `{{ }}`. ✅

15. What happened to `App.jsx` during the migration?

- It became `App.js`.
- It became `App.vue`. ✅
- It became `main.vue`.
- It was removed.

16. What happened to `main.jsx` during the migration?

- It became `main.css`.
- It became `App.vue`.
- It became `main.js`. ✅
- It became `index.js`.

17. Which React concepts were encountered during the migration?

- `useState`. ✅
- `useEffect`. ✅
- Props. ✅
- Redux.

18. Which Vue concepts were encountered during the migration?

- `ref`. ✅
- `onMounted`. ✅
- `defineProps`. ✅
- `useReducer`.

19. Which library replaces lucide-react in Vue?

- `vue-icons`.
- `lucide-vue-next`. ✅
- `heroicons-vue`.
- `vue-lucide-react`.

20. What is the primary purpose of reactive data?

- Styling components.
- Automatically updating the UI when data changes. ✅
- Creating API routes.
- Building production bundles.

21. During an AI-assisted migration, which actions remain the developer's responsibility?

- Reviewing generated code. ✅
- Validating functionality. ✅
- Understanding framework concepts. ✅
- Blindly accepting generated output.

22. Why was AI allowed during this project?

- To avoid learning Vue.js.
- To focus on understanding and comparison rather than manual rewriting. ✅
- To remove the need for testing.
- To replace documentation.

23. Which files would typically exist in the final Vue project?

- `App.vue`. ✅
- `main.js`. ✅
- `global.css`. ✅
- `package.json`. ✅

24. Which statement best describes the relationship between React and Vue.js?

- They solve completely different problems.
- Vue.js replaces React.
- They solve many of the same frontend problems using different approaches. ✅
- React is a backend framework.

25. What was the primary objective of this project?

- Building a better design than the React version.
- Creating a backend API.
- Deploying a production application.
- Learning Vue.js concepts and comparing them with React. ✅